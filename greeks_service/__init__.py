"""greeks-service — options greeks + carry-rate computation writing to the PricingLedger SSOT.

Package layout: ``kernels`` (Black-Scholes kernel + extensibility protocol), ``handlers``
(mark-update service handler), ``batch`` (backfill orchestration), ``api`` (service API entrypoint).
"""
